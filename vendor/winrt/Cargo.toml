# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g. crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
name = "winrt"
version = "0.4.0"
authors = ["Patrick Reisert", "Max Strini"]
exclude = ["Generator/**"]
description = "Automatically generated, (mostly) safe bindings for the Windows Runtime APIs"
homepage = "https://github.com/contextfree/winrt-rust"
documentation = "https://docs.rs/winrt/*/x86_64-pc-windows-msvc/winrt/"
readme = "README.md"
keywords = ["winrt", "uwp", "windows", "runtime", "com"]
categories = ["os::windows-apis", "api-bindings", "external-ffi-bindings"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/contextfree/winrt-rust"
[package.metadata.docs.rs]
default-target = "x86_64-pc-windows-msvc"
features = ["nightly"]
[dependencies.winapi]
version = "0.3"
features = ["winnt", "combaseapi", "oleauto", "roapi", "roerrorapi", "hstring", "winstring", "winerror", "restrictederrorinfo"]

[features]
all = ["windows-applicationmodel", "windows-data", "windows-devices", "windows-gaming", "windows-globalization", "windows-graphics", "windows-management", "windows-media", "windows-networking", "windows-perception", "windows-security", "windows-services", "windows-storage", "windows-system", "windows-ui", "windows-ui-xaml", "windows-web"]
lang-compat = []
nightly = []
windows-applicationmodel = []
windows-data = []
windows-devices = []
windows-gaming = []
windows-globalization = []
windows-graphics = []
windows-management = []
windows-media = []
windows-networking = []
windows-perception = []
windows-security = []
windows-services = []
windows-storage = []
windows-system = []
windows-ui = []
windows-ui-xaml = ["windows-ui"]
windows-web = []
